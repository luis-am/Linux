## Comando 'pip'

	pip help
	pip list
	pip search [PACKAGE_NAME]
	pip install [PACKAGE_NAME]
	pip uninstall [PACKAGE_NAME]
	pip list --outdated
	pip list -o
	pip install -U [PACKAGE_NAME]
	pip freeze > [TEXT_FILE]
	pip install -r [TEXT_FILE]
